<!--index.wxml-->
<view class="container">
  <van-collapse value="{{ activeNames }}" bind:change="onChange">
    <van-collapse-item wx:for="{{projectList}}" wx:key="index" title="{{item.name}}" name="{{item.id}}">
      
    </van-collapse-item>
  </van-collapse>
</view>