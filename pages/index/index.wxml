<!--index.wxml-->
<scroll-view scroll-y="true">
  <view wx:for="{{placeList}}" wx:key="id">
    <view data-id="{{item.id}}" data-mapId="{{item.mapId}}" data-name="{{item.name}}" class="list-item" bindtap="toMap">
      {{item.name}}
    </view>
  </view>
</scroll-view>
